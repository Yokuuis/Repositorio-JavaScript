//Que es una Arrays?

/*Las Arrays son como las variables, PERO, estas definiendo un "OBJETO", y tambien mientras en la variables guardas datos, en las arrays guardas varios
datos, tambien varias distintas de cosas. Y se utiliza con el signo ([]), y tambien la podemos hacer como una variable con cualquier tipo de nombre,
simplemente estamos expandiendo el texto con los "[]"
Ejemplo:*/

let arrays = ["Name",16,"pais"];
//Variable = let Arrays, [DATOS, OBJETOS, NUMEROS, ETC.], Asi seria una Array.
alert(arrays[1])
//tambien podemos especificar que dato queremos que se muestra primero, pero debemos de ponerlo del 0 hasta el numero de datos que tengamos.
document.write(arrays[0])
//asi de sencillo son las Arrays.